# Dependencies
node_modules/
package-lock.json

# Yarn Berry (keep releases + lockfile; ignore local cache)
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Environment variables
.env
!.env.example
.env.local
.env*.local

# Build outputs
.next/
out/
dist/
build/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Testing
coverage/
.nyc_output/

# Temporary files
*.tmp
*.temp

# Downloads
downloads/

# Uploaded research dashboards (per-symbol HTML)
backend/uploads/


# ── Generated data (skills/cowork jobs) ──────────────────────────────────────
# All generated data lives in jobs/data/ and is offloaded to Google Drive
# (StockMarket/jobs/v1) by packages/jobs-runtime/scripts/offloadToDrive.js.
# See skills/_shared/conventions.md §6–7. Data files must NEVER be committed.
jobs/data/
delivery_cache/

# Legacy stray output dirs at the repo root (written by older skill runs whose
# env resolution fell back to process.cwd(); kept ignored as a guard)
/daily_gainers/
/notes/
/validation/
/events/
/entities/
/documents/
/data/*.html

# Legacy cowork-jobs paths (pre jobs-runtime relocation) — guard against revival
packages/cowork-jobs/
packages/cowork-jobs/process_watchlist.js
