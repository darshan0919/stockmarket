CATEGORY: demerger [HIGH CONVICTION]
Extract: which segment(s) are being demerged, the resulting company name(s),
share-exchange ratio (e.g. 1:1), record date if set, and each entity's core
business post-split. State the market's likely re-rating rationale (which
segment was getting the conglomerate discount, and why). Note how many weeks/
months since the announcement — if >2 weeks old, say explicitly that the
initial re-rating window has likely already closed and this is now a
fundamental call. significance floor: medium. Tag `high_conviction`.
For the full valuation/timing framework, see
`references/demerger-merger-management-change-playbook.md` Part 1 — use `--depth deep`
to get the full SOTP treatment.
